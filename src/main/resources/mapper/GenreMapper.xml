<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd ">
  
  <mapper namespace="kr.co.purplaying.dao.genreMapper">
	<select id="genreLiterature" parameterType="map" resultType="genreDto" >
	select 
	prdt_genre,
	prdt_name,
	(prdt_currenttotal/prdt_goal)*100 as "prdt_percent",
	prdt_currenttotal, 
	(prdt_enddate-prdt_opendate) as "prdt_dday"
	from tb_product
	where prdt_genre = 1 and <![CDATA[prdt_opendate <= current_date ]]>
	limit 6
	</select>
	
	<select id="genrePoemEssay" parameterType="map" resultType="genreDto" >
	select 
	prdt_genre,
	prdt_name,
	(prdt_currenttotal/prdt_goal)*100 as "prdt_percent",
	prdt_currenttotal, 
	(prdt_enddate-prdt_opendate) as "prdt_dday"
	from tb_product
	where prdt_genre = 2 and <![CDATA[prdt_opendate <= current_date ]]>
	limit 6
	</select>
	
	<select id="genreWebtoon" parameterType="map" resultType="genreDto" >
	select 
	prdt_genre,
	prdt_name,
	(prdt_currenttotal/prdt_goal)*100 as "prdt_percent",
	prdt_currenttotal, 
	(prdt_enddate-prdt_opendate) as "prdt_dday"
	from tb_product
	where prdt_genre = 3 and <![CDATA[prdt_opendate <= current_date ]]>
	limit 6
	</select>
</mapper>
  
  