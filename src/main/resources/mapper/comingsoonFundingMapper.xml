<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd ">

<mapper namespace="kr.co.purplaying.dao.comingsoonFundingMapper">
	<select id="comingsoonFunding" parameterType="map" resultType="comingsoonFundingDto" >
	select 
	prdt_genre,
	prdt_name,
	prdt_opendate,
	(prdt_opendate - current_date) as "prdt_comingday"
	from tb_product
	where <![CDATA[prdt_opendate > current_date]]>
	order by prdt_comingday, prdt_id desc
	limit 6;
	</select>
</mapper>