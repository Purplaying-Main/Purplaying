<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd ">
  
<mapper namespace="kr.co.purplaying.dao.CommunityMapper">
	<sql id="selectFromCommunity">
		SELECT cmnt_rno, cmnt_writer, cmnt_content, cmnt_regdate, product_id, user_no
		FROM tb_community;
	</sql>
	
	<update id="update" parameterType="CommunityDto">
		UPDATE tb_community
		SET cmnt_writer=#{cmnt_writer}, cmnt_content=#{cmnt_coontent}, cmnt_regdate=now(), product_id=#{product_id}, user_no=#{user_no}
		WHERE cmnt_rno=#{bno};
	</update>
	
</mapper>