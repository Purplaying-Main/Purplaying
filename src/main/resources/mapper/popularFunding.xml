<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd ">

<mapper namespace="kr.co.purplaying.dao.popularFundingMapper">
	<select id="popularFunding" parameterType="map" resultType="popularFundingDto" >
		select 
		prdt_gerne,
		prdt_name,
		(prdt_currenttotal/prdt_goal)*100 as "prdt_percent",
		prdt_currenttotal, 
		(prdt_enddate-prdt_opendate) as "prdt_dday",
		row_number() over(order by (prdt_currenttotal/prdt_goal)*100 desc)
		from tb_product
		limit 6
	</select>
</mapper>